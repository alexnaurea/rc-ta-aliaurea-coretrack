import { StepLogger } from '../../../../../core/logger/step-logger';
import { PageHelper } from '../../../../components/html/page-helper';
import { ContactPageHelper } from '../../../../page-objects/pages/contact-page/contact-page.helper';
import { HomePageHelper } from '../../../../page-objects/pages/home-page/home-page.helper';
import { LoginPageHelper } from '../../../../page-objects/pages/login-page/login-page.helper';
import { NewProspectPageHelper } from '../../../../page-objects/pages/new-prospect-page/new-prospect-page.helper';
import { SuiteNames } from '../../../helpers/suite-names';

describe(SuiteNames.regressionSuite, () => {
    let loginPageHelper: LoginPageHelper;

    beforeAll(async () => {
        loginPageHelper = LoginPageHelper.getInstance();
        await PageHelper.maximiseBrowser();
    });

    beforeEach(async () => {
        await PageHelper.restartBrowser();
        await loginPageHelper.goTo();
    });

    // Jira References - COTRAC-2072
    it('Verify user is able to access Relationship Pane of a Retail Prospect - [22980656]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:37 GMT

        StepLogger.caseId = 22980656;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();
    });

    // Jira References - COTRAC-2072
    it('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect - [22980708]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:36 GMT

        StepLogger.caseId = 22980708;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();
    });

    // Jira References - COTRAC-2072
    it('Verify sections present in Summary tab on the Relationship Pane of a Retail Prospect - [22980735]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:37 GMT

        StepLogger.caseId = 22980735;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();

        StepLogger.stepId(8);
        StepLogger.step('Verify sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The following sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabOptionsDisplayedInRelationshipPane();
    });

    // Jira References - COTRAC-2072
    it('Verify Accounts section listed under the ACCOUNTS AND OPPORTUNITIES in Summary tab - [22980757]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:36 GMT

        StepLogger.caseId = 22980757;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();

        StepLogger.stepId(8);
        StepLogger.step('Verify sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The following sections present in Summary tab on the Relationship Pane of a Retail Prospect:');
        await ContactPageHelper.verifySummaryTabOptionsDisplayedInRelationshipPane();

        StepLogger.stepId(9);
        StepLogger.step('Verify Accounts section listed under the ACCOUNTS AND OPPORTUNITIES in Summary tab');
        StepLogger.verification('The Accounts section listed under the ACCOUNTS AND OPPORTUNITIES in Summary tab');
        await ContactPageHelper.verifyAccountSection();
    });

    // Jira References - COTRAC-2072
    it('Verify Opportunities section listed under the ACCOUNTS AND OPPORTUNITIES in Summary tab - [22980747]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:37 GMT

        StepLogger.caseId = 22980747;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();

        StepLogger.stepId(8);
        StepLogger.step('Verify sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The following sections present in Summary tab on the Relationship Pane of a Retail Prospect:');
        await ContactPageHelper.verifySummaryTabOptionsDisplayedInRelationshipPane();

        StepLogger.stepId(9);
        StepLogger.step('Verify Opportunities section listed under the ACCOUNTS AND OPPORTUNITIES in Summary tab');
        StepLogger.verification(
            'The Opportunities section listed under the ACCOUNTS AND OPPORTUNITIES in Summary tab has following fields '
        );
        await ContactPageHelper.verifyOpportunitiesSection();
    });

    // Jira References - COTRAC-2072
    it('Verify Services section listed in Summary tab of Relationship Pane - [22980765]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:37 GMT

        StepLogger.caseId = 22980765;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();

        StepLogger.stepId(8);
        StepLogger.step('Verify sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The following sections present in Summary tab on the Relationship Pane ');
        await ContactPageHelper.verifySummaryTabOptionsDisplayedInRelationshipPane();

        StepLogger.stepId(9);
        StepLogger.step('Verify Services section listed in Summary tab of Relationship Pane');
        StepLogger.verification('The Services section listed in Summary tab ');
        await ContactPageHelper.verifyServiceSection();
    });

    // Jira References - COTRAC-2072
    it('Verify Overview Options present in Summary tab on the Relationship Pane of a Retail Prospect - [22980899]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:56 GMT

        StepLogger.caseId = 22980899;
        StepLogger.preCondition('Existing Retail Prospect/Contact is added, C22991222');
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();

        StepLogger.stepId(8);
        StepLogger.step('Verify Overview Options present in Summary tab on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Overview Options present in Summary tab on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifyOverviewOptionsSection();
    });

    // Jira References - COTRAC-2072
    it('Verify Closed Accounts section listed in Summary tab of Relationship Pane - [22980780]', async () => {
        // Auto generated by aurea-automation - util on Tue, 02 Apr 2019 17:05:56 GMT

        StepLogger.caseId = 22980780;
        const uniqueName = PageHelper.getUniqueId();
        await NewProspectPageHelper.navigateAndAddExistingProspect(uniqueName);

        StepLogger.stepId(1);
        StepLogger.step('Click on global search button.');
        await HomePageHelper.clickSearchButton();
        StepLogger.verification('The Search text field is displayed.');
        await HomePageHelper.verifySearchBox();

        StepLogger.stepId(2);
        StepLogger.step('Select Contacts from the dropdown next to search text field.');
        await HomePageHelper.selectContactsFromSearchDropdown();
        StepLogger.verification('The Contacts option is selected from the dropdown.');
        await HomePageHelper.verifyContactsOptionIsSelectedInDropdown();

        StepLogger.stepId(3);
        StepLogger.step('Enter the name of the Retail Prospect on the search bar and click on the search button e.g. Baldwin, Alec');
        await HomePageHelper.enterSearchTextAndClicksearch(uniqueName);
        StepLogger.verification('The Contact card is displayed on the screen.');
        await HomePageHelper.verifyContactCardIsDisplayed(uniqueName);

        StepLogger.stepId(4);
        StepLogger.step('Click on the Retail Prospect Contact card.');
        await HomePageHelper.clickOnSearchResult(uniqueName);
        StepLogger.verification('The Snapshot window of the Contact is displayed.');
        const isPresent = await ContactPageHelper.verifySnapshotModal(false);

        StepLogger.stepId(5);
        StepLogger.step('Close the Snapshot Window');
        await ContactPageHelper.closeSnapshot(isPresent);
        StepLogger.verification('The details of the contact is displayed on a new page with Overview tab displayed by default.');
        await ContactPageHelper.verifyOverviewIsDisplayed(false);

        StepLogger.stepId(6);
        StepLogger.step('Click on Relationship button in the left side navigation pane.');
        await ContactPageHelper.clickRelationshipTab();
        StepLogger.verification('The screen is refreshed and the Relationship page is displayed on the right side pane.');
        await ContactPageHelper.verifyRelationshipTabIsDisplayed();

        StepLogger.stepId(7);
        StepLogger.step('Verify Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The Summary tab is displayed by default on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabIsSelected();

        StepLogger.stepId(8);
        StepLogger.step('Verify sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        StepLogger.verification('The following sections present in Summary tab on the Relationship Pane of a Retail Prospect');
        await ContactPageHelper.verifySummaryTabOptionsDisplayedInRelationshipPane();

        StepLogger.stepId(9);
        StepLogger.step('Verify Closed Accounts section listed in Summary tab of Relationship Pane');
        StepLogger.verification('The Closed Accounts section listed in Summary tab of Relationship Pane ');
        await ContactPageHelper.verifyClosedAccountsSection();
    });
});
